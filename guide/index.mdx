# PokeSVG

PokeSVG is a Pokedex-style web app where every creature is generated as SVG from a deterministic `genome` (JSON).

This guide will be expanded as the app is implemented phase-by-phase.
<!-- unpack:1.0.0 -->
